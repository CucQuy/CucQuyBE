name: build-and-deploy

# Merge/push vào staging hoặc production → CI gate (tsc) → build image amd64
# (native trên runner ubuntu) → push GHCR. KHÔNG deploy ở đây:
# keel (k3s @ ricevps) poll GHCR @every 1m (policy=force) và TỰ rollout:
#   - production → :latest  → ns cucquy           (api.cucquy.site)
#   - staging    → :staging → ns cucquy-staging   (api-staging.cucquy.site)
# Backend config-driven (DATABASE_URL/secret lấy từ k8s) → 1 image cho cả 2 môi trường.
# Không cần kubeconfig/SSH trong CI. Push GHCR bằng GITHUB_TOKEN (packages: write).
on:
  push:
    branches: [staging, production]
  workflow_dispatch: {}

concurrency:
  group: deploy-backend-${{ github.ref_name }}
  cancel-in-progress: true

env:
  IMAGE: ghcr.io/cucquy/cucquy-backend

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npx tsc --noEmit

  build-push:
    needs: ci
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Resolve tag theo nhánh
        id: env
        run: |
          if [ "${{ github.ref_name }}" = "production" ]; then
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          else
            echo "tag=staging" >> "$GITHUB_OUTPUT"
          fi
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ steps.env.outputs.tag }}
            ${{ env.IMAGE }}:${{ github.sha }}
      - run: echo "Pushed ${IMAGE}:${{ steps.env.outputs.tag }} (+sha). keel tự rollout trong ~1 phút."
